import React from 'react';
import { Text, View } from 'react-native';

const coffee = () => {
  return (
    <View className='flex-1 items-center justify-center'>
      <Text className='text-2xl font-bold'>coffee</Text>
    </View>
  );
};

export default coffee;
